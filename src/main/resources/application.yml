# Spring
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5432/personalbudget
    username: postgres
    password: 12345
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: personal.budget.java.app@gmail.com
    password: fxzd jcpb wfpl kbpw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Tomcat
server:
  error:
    include-exception: true
    include-message: always
    include-stacktrace: always
